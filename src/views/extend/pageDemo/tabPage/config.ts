// TODO: 设置页签的`key`的枚举
/**
 * @description tabs类型枚举
 */
export enum TABS_ENUM {
  待处理 = '0',
  已处理 = '1',
}

// TODO: 设置容器的`id`，根据业务内容创建一个唯一的`id`
/** 页面容器`id`，用于各种弹窗组件的转移 */
export const DOM_CONTAINER_ID = 'tabs-container';
